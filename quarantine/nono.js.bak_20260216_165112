#!/usr/bin/env node
import { run } from "../src/run.js";

async function main() {
  const args = process.argv.slice(2);

  // Placeholder: enterprise mode not yet implemented
  if (args[0] === "enterprise") {
    console.error("Enterprise mode not implemented yet.");
    process.exit(1);
  }

  console.error("Unknown command.");
  process.exit(1);
}

main().catch(e => {
  console.error(e.message);
  process.exit(1);
});
