export function diffIssues(current, baseline){ const baseIds=new Set((baseline||[]).map(i=>i.identity)); const currIds=new Set((current||[]).map(i=>i.identity)); const newIssues=current.filter(i=>!baseIds.has(i.identity)); const resolved=(baseline||[]).filter(i=>!currIds.has(i.identity)); const unchanged=current.filter(i=>baseIds.has(i.identity)); return { new:newIssues, resolved, unchanged }; }
